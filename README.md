# Banco de prueba · .NET

Qué ejercita: **versiones centralizadas** (`Directory.Packages.props`). El `.csproj` no dice la versión — si el lector no resuelve la herencia, no detecta nada y el fallo es silencioso.
